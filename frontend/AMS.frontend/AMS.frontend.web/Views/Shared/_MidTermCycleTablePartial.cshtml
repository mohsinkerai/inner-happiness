@model AMS.frontend.web.Areas.Operations.Models.Cycle.MidTermCycle
<table id="midTermCycle" class="table table-striped- table-bordered table-hover">
    <thead>
        <tr>
            <th> Institution </th>
            <th> Position </th>
            <th> Start Date </th>
            
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            /*foreach (var item in Model)
            {
                <tr id="education-row-">
                    <td> @item.Institution </td>
                    <td> @item.Position </td>
                    <td> @item.StartDate </td>
                </tr>
            }*/
        }
    </tbody>
</table>